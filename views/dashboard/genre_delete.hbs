{{> dashboard_nav}}
<div class="card">
    <div class="card-body">
        <form method="post" action="/dashboard/genre/delete">
            <h5 class="card-title">Are you sure to delete <span class="fs-5 text-danger">{{genre}} </span></h5>
            <input type="hidden" name="genreId" value={{genreId}}>
            <input type="submit" class="btn btn-danger btn-sm" value="Delete">
    </div>
</div>
{{> dashboard_footer}}